import React from 'react';
import {View} from 'react-native';

interface BleParamFormProps {}

const BleParamForm = ({}: BleParamFormProps) => {
  return <View></View>;
};

export default BleParamForm;
